"use client";

import { ChoiceStepper } from "./choice-stepper/choice-stepper";

/** Compatibility entry point retained for the route and existing imports. */
export function ChoiceForm() {
  return <ChoiceStepper />;
}
